<UserControl x:Class="EndoscopyApp.Views.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:EndoscopyApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16">
            <TextBlock Text="Live View" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
        </materialDesign:ColorZone>

        <!-- Video Feed -->
        <Border Grid.Row="1" Background="Black" CornerRadius="4" Margin="10">
            <Image Source="{Binding CurrentFrame}" Stretch="Uniform"/>
        </Border>

        <!-- Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Command="{Binding StartCameraCommand}" Content="Start Camera" Margin="0,0,10,0" IsEnabled="{Binding IsCameraRunning, Converter={StaticResource InverseBooleanConverter}}"/>
            <Button Command="{Binding StopCameraCommand}" Content="Stop Camera" Style="{StaticResource MaterialDesignOutlinedButton}" IsEnabled="{Binding IsCameraRunning}"/>
        </StackPanel>
    </Grid>
</UserControl>
