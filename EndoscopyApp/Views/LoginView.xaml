<UserControl x:Class="EndoscopyApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:EndoscopyApp.ViewModels"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <Grid>
        <materialDesign:Card Width="300" Padding="20" VerticalAlignment="Center" UniformCornerRadius="10">
            <StackPanel>
                <TextBlock Text="Endoscopy Capture" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="Please Login" Style="{StaticResource MaterialDesignBody1TextBlock}" HorizontalAlignment="Center" Opacity="0.6" Margin="0,0,0,15"/>

                <PasswordBox x:Name="UserPasswordBox" 
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,15"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,15"/>

                <Button Command="{Binding LoginCommand}" Content="LOGIN" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
