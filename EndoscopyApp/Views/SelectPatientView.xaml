<UserControl x:Class="EndoscopyApp.Views.SelectPatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EndoscopyApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="#F7FAFC">
    <Grid Margin="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Command="{Binding NavigateBackCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Width="45" Height="45" Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24" Foreground="Black"/>
                </Button>
                <TextBlock Text="Live Video" FontSize="30" FontWeight="ExtraBold" VerticalAlignment="Center" Foreground="#1A202C"/>
            </StackPanel>
        </Grid>

        <!-- Patient Selection Container -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Select a Patient" FontSize="20" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,24"/>
                
                <ItemsControl ItemsSource="{Binding Patients}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectPatientCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Padding="0" Margin="10" Height="Auto" HorizontalContentAlignment="Stretch">
                                <materialDesign:Card Background="White" UniformCornerRadius="12" Padding="20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Width="45" Height="45" Background="#EBF4FF" CornerRadius="22.5" VerticalAlignment="Top">
                                            <materialDesign:PackIcon Kind="AccountOutline" Foreground="#4299E1" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#2D3748"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock Text="ID: " Foreground="#718096" FontSize="13"/>
                                                <TextBlock Text="{Binding Id, StringFormat='{}{0:D6}'}" Foreground="#718096" FontSize="13"/>
                                            </StackPanel>
                                            
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                <TextBlock Text="{Binding Age}" Foreground="#4A5568" FontSize="14"/>
                                                <TextBlock Text=" years" Foreground="#4A5568" FontSize="14" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Gender}" Foreground="#4A5568" FontSize="14"/>
                                            </StackPanel>
                                            
                                            <TextBlock Text="{Binding Phone}" Foreground="#4A5568" FontSize="14" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </materialDesign:Card>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
