<UserControl x:Class="EndoscopyApp.Views.MediaViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="#F7FAFC">
    <Grid Margin="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Command="{Binding NavigateBackCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Padding="0" Width="45" Height="45" Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="28" Height="28"/>
                </Button>
                <TextBlock Text="{Binding Media.FileName}" FontSize="30" FontWeight="ExtraBold" VerticalAlignment="Center" Foreground="#1A202C"/>
            </StackPanel>
        </Grid>

        <!-- Content Card -->
        <materialDesign:Card Grid.Row="1" Background="Black" UniformCornerRadius="12">
            <Grid>
                <!-- Image Viewer -->
                <Image Source="{Binding Media.FilePath}" Stretch="Uniform">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Media.IsVideo}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>

                <!-- Video Player -->
                <MediaElement Source="{Binding Media.FilePath}" 
                              LoadedBehavior="Play"
                              UnloadedBehavior="Stop"
                              Stretch="Uniform">
                    <MediaElement.Style>
                        <Style TargetType="MediaElement">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Media.IsVideo}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MediaElement.Style>
                </MediaElement>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
