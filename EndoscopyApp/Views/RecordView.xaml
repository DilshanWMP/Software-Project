<UserControl x:Class="EndoscopyApp.Views.RecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:EndoscopyApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Patient Details -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
            <StackPanel Margin="16">
                <TextBlock Text="Patient Details" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>
                
                <TextBox Text="{Binding PatientName}" materialDesign:HintAssist.Hint="Full Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" IsEnabled="{Binding IsPatientRegistered, Converter={StaticResource InverseBooleanConverter}}"/>
                <TextBox Text="{Binding PatientAge}" materialDesign:HintAssist.Hint="Age" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" IsEnabled="{Binding IsPatientRegistered, Converter={StaticResource InverseBooleanConverter}}"/>
                
                <ComboBox ItemsSource="{Binding GenderOptions}" SelectedItem="{Binding PatientGender}" materialDesign:HintAssist.Hint="Gender" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,16" IsEnabled="{Binding IsPatientRegistered, Converter={StaticResource InverseBooleanConverter}}"/>
                
                <TextBox Text="{Binding PatientPhone}" materialDesign:HintAssist.Hint="Phone Number" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,24" IsEnabled="{Binding IsPatientRegistered, Converter={StaticResource InverseBooleanConverter}}"/>

                <Button Command="{Binding StartSessionCommand}" Content="START SESSION" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,0,16" IsEnabled="{Binding IsPatientRegistered, Converter={StaticResource InverseBooleanConverter}}"/>
                
                <Separator Margin="0,16"/>
                
                <TextBlock Text="Controls" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16"/>
                
                <Button Command="{Binding TakeSnapshotCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,0,16" IsEnabled="{Binding IsCameraRunning}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Camera" Margin="0,0,8,0"/>
                        <TextBlock Text="Snapshot"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ToggleRecordingCommand}" Style="{StaticResource MaterialDesignRaisedAccentButton}" IsEnabled="{Binding IsCameraRunning}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Record" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding IsRecording, Converter={StaticResource BooleanToStringConverter}, ConverterParameter=Stop Recording|Start Recording}"/> 
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Right Panel: Camera Feed -->
        <Grid Grid.Column="1" Background="Black">
            <Image Source="{Binding CurrentFrame}" Stretch="Uniform"/>
            
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Right" Margin="16" Orientation="Horizontal">
                 <materialDesign:Chip Content="REC" Background="Red" Foreground="White" Visibility="{Binding IsRecording, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
